<UserControl x:Class="StreamMesh.UI.Views.SearchAceStreamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="720" d:DesignWidth="1000">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🌐 Global Medya ve Kanal Arama" FontSize="30" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Arama kaynağını seçin (AceStream Engine, Search Web, Net Web, IPTVCat, FreeTuxTV veya Tümü) ve kanalları tek tıkla kütüphanenize aktarın." FontSize="13" Foreground="#94a3b8" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <!-- Üst Filtre Satırı: Kaynak, Kategori, Dil -->
            <Grid Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Kaynak Seçimi (Source Selector) -->
                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="Kaynak" Foreground="#94a3b8" FontSize="12" Margin="0,0,0,6" FontWeight="SemiBold"/>
                    <ComboBox x:Name="SourceComboBox" Height="44" FontSize="13" SelectedIndex="0">
                        <ComboBoxItem Content="🌐 Tüm Kaynaklar" IsSelected="True"/>
                        <ComboBoxItem Content="⚡ AceStream Engine (Yerel/Ağ API)"/>
                        <ComboBoxItem Content="🔍 AceStream Search Web (search-ace.stream)"/>
                        <ComboBoxItem Content="🌐 AceStream Net Web (ace-stream.net)"/>
                        <ComboBoxItem Content="🐱 IPTVCat Arama Motoru"/>
                        <ComboBoxItem Content="🐧 FreeTuxTV Veritabanı"/>
                    </ComboBox>
                </StackPanel>

                <!-- Kategori Seçimi -->
                <StackPanel Grid.Column="1" Margin="4,0,8,0">
                    <TextBlock Text="Kategori" Foreground="#94a3b8" FontSize="12" Margin="0,0,0,6" FontWeight="SemiBold"/>
                    <ComboBox x:Name="CategoryComboBox" Height="44" FontSize="13" SelectedIndex="0">
                        <ComboBoxItem Content="🏷️ Tüm Kategoriler" IsSelected="True"/>
                        <ComboBoxItem Content="🎬 Filmler"/>
                        <ComboBoxItem Content="🍿 Diziler"/>
                        <ComboBoxItem Content="📺 Canlı TV"/>
                        <ComboBoxItem Content="⚽ Spor"/>
                        <ComboBoxItem Content="🧬 Belgesel &amp; Eğitim"/>
                        <ComboBoxItem Content="🎵 Müzik"/>
                        <ComboBoxItem Content="📰 Haber"/>
                        <ComboBoxItem Content="🧸 Çocuk"/>
                    </ComboBox>
                </StackPanel>

                <!-- Dil Seçimi -->
                <StackPanel Grid.Column="2" Margin="4,0,0,0">
                    <TextBlock Text="Dil / Ülke" Foreground="#94a3b8" FontSize="12" Margin="0,0,0,6" FontWeight="SemiBold"/>
                    <ComboBox x:Name="LanguageComboBox" Height="44" FontSize="13" SelectedIndex="0">
                        <ComboBoxItem Content="🌍 Tüm Diller" IsSelected="True"/>
                        <ComboBoxItem Content="🇹🇷 Türkçe"/>
                        <ComboBoxItem Content="🇬🇧 İngilizce"/>
                        <ComboBoxItem Content="🇩🇪 Almanca"/>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <!-- Arama Kutusu ve Butonlar -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Arama Kutusu -->
                <TextBox Grid.Column="0" x:Name="SearchBox" Height="45" Padding="15,10" FontSize="14" KeyDown="SearchBox_KeyDown" Tag="Film, dizi, kanal adı veya AceStream Content ID..."/>

                <!-- Arama Butonu -->
                <Button Grid.Column="1" Content="🔍 Arama Yap" Width="130" Height="45" Margin="12,0,0,0" Style="{StaticResource PrimaryButtonStyle}" Click="Search_Click"/>

                <!-- Tümünü Kütüphaneye Ekle Butonu -->
                <Button Grid.Column="2" x:Name="AddAllButton" Content="➕ Tümünü Kütüphaneye Ekle" Height="45" Padding="15,0" Margin="12,0,0,0" Style="{StaticResource PrimaryButtonStyle}" Background="#0284c7" Visibility="Collapsed" Click="AddAllToLibrary_Click"/>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="2">
            <ListBox x:Name="ResultsList" Background="Transparent" BorderThickness="0" ItemsSource="{Binding Results}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#1e293b" CornerRadius="12" Margin="0,0,0,12" Padding="16" BorderBrush="#334155" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <Border Background="#0284c7" CornerRadius="6" Padding="8,3" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding Source}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                        </Border>
                                        <Border Background="#334155" CornerRadius="6" Padding="8,3">
                                            <TextBlock Text="{Binding Category}" Foreground="#cbd5e1" FontSize="11"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="17" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Url}" Foreground="#64748b" FontSize="11" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding PeersOrDetails}" Foreground="#10b981" FontSize="12" FontWeight="Bold" Margin="0,4,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="▶ Oynat" Width="90" Height="38" Margin="0,0,8,0" Style="{StaticResource PrimaryButtonStyle}" Background="#16a34a" Click="PlayResult_Click" CommandParameter="{Binding}"/>
                                    <Button Content="+ Kütüphaneye Ekle" Height="38" Padding="12,0" Style="{StaticResource PrimaryButtonStyle}" Background="#334155" Click="AddToLibrary_Click" CommandParameter="{Binding}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Loading Overlay -->
            <Border x:Name="LoadingBar" Background="#CC000000" Visibility="Collapsed" CornerRadius="12">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="240" Height="6" Foreground="#38bdf8" Background="#1e293b" BorderThickness="0"/>
                    <TextBlock Text="Seçilen Kaynaklarda Aranıyor..." Foreground="White" FontSize="14" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
